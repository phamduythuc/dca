import { UppercasePipePipe } from './uppercase-pipe.pipe';

describe('UppercasePipePipe', () => {
  it('create an instance', () => {
    const pipe = new UppercasePipePipe();
    expect(pipe).toBeTruthy();
  });
});
